const numbers = [1, 12, 4, 18, 9, 7, 11, 3, 50, 5, 6];
const test = 2;
// for (let number = 0; number < numbers.length; number++) {
//   console.log(numbers[number]);
// }

for (let number = 0; number < numbers.length; number++) {
  if (numbers[number] > 5) {
    // console.log(numbers[number]);
  }
}

const greaterThanTen = [];

for (number of numbers) {
  if (number > 10) {
    greaterThanTen.unshift(number);
  }
}

const multypliedByFive = [];

for (number of numbers) {
  multypliedByFive.push(number * 5);
}

console.log(multypliedByFive);
