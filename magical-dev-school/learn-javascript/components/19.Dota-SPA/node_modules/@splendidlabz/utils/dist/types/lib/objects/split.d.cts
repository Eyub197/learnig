/**
 * Split on object up based on the keys provided.
 * @param {Object} obj
 * @param {Array} keys
 * @returns
 */
declare function splitObject(obj: any, keys: any[]): {
    picked: {};
    omitted: any;
    p: {};
    o: any;
};

export { splitObject };
