// We need one container to store it all
// them we have a sub container for each accordion
// in that sub container we have a head container and content container
// Whenever we click head container we switch the height of the content
// form 0 to 100%
// we need a class like is-open. Whenever is-open is present they will expand to fill up that space
// we need to get all of the headers
// add an click event to all of the arrows
// then whenver that arrow is click we add the class to that accordion

import "./accordion.js";
