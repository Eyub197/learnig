// List of countries.
// Scroll past this list to code.
// Start writing JavaScript here!
// whenver we type something we need to get that data
// from that data we need to check what starts with the typed letters
// whatever srarts with dose letters we need to show it to the user
// from those showed things we need an event listener that when we click on an input li we get it's textcontent to the input and we remove the
// ul and we set the value of the input
// then we need to bold the search terms

import Typeahead from "./typeahead.js";
await Typeahead(document.querySelector(".typeahead"));
